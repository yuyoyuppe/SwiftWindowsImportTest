# Description
This is a executable application demonstrating a use case where we have a closed-source vendor library ([`HelloModule`](./HelloModule/)) distributed as a Windows binary (`.lib`/`.dll`/`.swiftinterface`) which we want to consume ([`ClientApp`](./ClientApp/)).